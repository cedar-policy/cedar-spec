permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.level <= 0 };

forbid (
    principal,
    action == Action::"access",
    resource
)
when { resource.level > 49 };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.name == "resource_2" };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.level <= 3 };

forbid (
    principal,
    action == Action::"access",
    resource
)
when { resource.level > 46 };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.name == "resource_5" };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.level <= 6 };

forbid (
    principal,
    action == Action::"access",
    resource
)
when { resource.level > 43 };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.name == "resource_8" };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.level <= 9 };

forbid (
    principal,
    action == Action::"access",
    resource
)
when { resource.level > 40 };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.name == "resource_11" };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.level <= 12 };

forbid (
    principal,
    action == Action::"access",
    resource
)
when { resource.level > 37 };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.name == "resource_14" };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.level <= 15 };

forbid (
    principal,
    action == Action::"access",
    resource
)
when { resource.level > 34 };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.name == "resource_17" };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.level <= 18 };

forbid (
    principal,
    action == Action::"access",
    resource
)
when { resource.level > 31 };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.name == "resource_20" };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.level <= 21 };

forbid (
    principal,
    action == Action::"access",
    resource
)
when { resource.level > 28 };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.name == "resource_23" };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.level <= 24 };

forbid (
    principal,
    action == Action::"access",
    resource
)
when { resource.level > 25 };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.name == "resource_26" };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.level <= 27 };

forbid (
    principal,
    action == Action::"access",
    resource
)
when { resource.level > 22 };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.name == "resource_29" };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.level <= 30 };

forbid (
    principal,
    action == Action::"access",
    resource
)
when { resource.level > 19 };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.name == "resource_32" };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.level <= 33 };

forbid (
    principal,
    action == Action::"access",
    resource
)
when { resource.level > 16 };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.name == "resource_35" };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.level <= 36 };

forbid (
    principal,
    action == Action::"access",
    resource
)
when { resource.level > 13 };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.name == "resource_38" };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.level <= 39 };

forbid (
    principal,
    action == Action::"access",
    resource
)
when { resource.level > 10 };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.name == "resource_41" };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.level <= 42 };

forbid (
    principal,
    action == Action::"access",
    resource
)
when { resource.level > 7 };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.name == "resource_44" };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.level <= 45 };

forbid (
    principal,
    action == Action::"access",
    resource
)
when { resource.level > 4 };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.name == "resource_47" };

permit (
    principal,
    action == Action::"access",
    resource
)
when { resource.level <= 48 };

forbid (
    principal,
    action == Action::"access",
    resource
)
when { resource.level > 1 };
